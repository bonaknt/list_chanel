{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<header class="section">
    <div class="row justify-content-center pt-4">
        <h1 class="text-center">Les chaines YouTube française sur les cryptomonnaies</h1>
    </div>
</header>
<section class="section">
    <div class="row justify-content-center pt-3" id="titleList">
        <h2 class="text-center display-3" id="list">La liste</h2>
    </div>
    <div>
    {% for chanel in listChanel %}
        <div class="blockList col-10 offset-1 mb-3">
            <div class="row">
                <div class="col-6 offset-3">
                    <h2 class="text-center">{{ chanel.name }}</h2>
                </div>
            </div>
            <div class="row">
                <div class="col-10 offset-1">
                    <p>{{ chanel.description | nl2br}}</p>
                </div>
            </div>
            <div class="row">
                <div class="col-4 offset-4">
                    <h3 class="text-center"><a href='{{ chanel.url }}'>Regarder cette chaine</a></h3>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>
</section>
{% endblock %}
